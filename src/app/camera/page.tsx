
import CameraInterface from '@/components/app/camera/CameraInterface';

export default function CameraPage() {
  return (
    // The CameraInterface component will handle its own layout, often full-screen
    <CameraInterface />
  );
}
