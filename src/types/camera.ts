
export type CameraMode = 'photo' | 'story_photo' | 'story_video' | 'reel' | 'live' | 'vlog';

// Placeholder for other camera related types if needed in future
// export interface FilterEffect {
//   id: string;
//   name: string;
//   type: 'filter' | 'ar_lens' | 'beauty';
//   // other properties like thumbnail, shader code, etc.
// }

// export interface MusicTrack {
//   id: string;
//   title: string;
//   artist?: string;
//   url: string;
//   duration: number;
// }
